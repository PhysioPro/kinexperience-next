echo '{
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}' > vercel.json
git add vercel.json
git commit -m "Ajout fichier vercel.json pour fix 404 Vercel"
git push origin main
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
